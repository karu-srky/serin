import React from "react";

import { Button } from "@mui/material"

const frameButton = (props: any) => {
    return(
        <>
            <Button>
                
            </Button>
        </>
    )
}